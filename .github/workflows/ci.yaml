name: Build and Push Docker Images

on:
  push:
    paths:
      - src/**/Dockerfile
      - .github/workflows/ci.yaml

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v3

      - name: Set AWS Credentials
        run: |
          export AWS_ACCESS_KEY_ID=ASIATFVAV3TBIKRJTWQL
          export AWS_SECRET_ACCESS_KEY=yJ/H3MERekDmwesTWozN62duzoeI8iJZzCpQoTDr
          export AWS_REGION=ap-southeast-1

      - name: Login to Amazon ECR
        run: |
          aws ecr get-login-password --region ap-southeast-1 | docker login --username AWS --password-stdin 218306567362.dkr.ecr.ap-southeast-1.amazonaws.com
